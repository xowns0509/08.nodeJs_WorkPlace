<!DOCTYPE html>
<html>
  <head>
  <meta charset="utf-8">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <a href="/write">글쓰기</a><br/>
    <table width='500' border='1'>
    	<tr>
    		<th>번호</th><th>제목</th><th>조회수</th><th>작성일자</th>
    	</tr>
    	<% for( var i=0; i < rows.length; i++ ) { %>
    	<tr>
    		<td><%= max-i %></td>
    		<td><a href="/read/<%=rows[i].num%>/<%=page%>"><%= rows[i].title %></a></td>
    		<td><%= rows[i].hit %></td>
    		<td><%= rows[i].regdate %></td>
    	</tr>
    	<% } %>
      <div style="width:40em;text-aling;center;padding-top:2px;">
          <% if( page > pageSize) {%>
            [ <a style="text-decoration:none" href="/list/1"> ◀◀</a>]
            [ <a style="text-decoration:none" href="/list/<%=startPage-1%>"> ◀</a>]
          <% } else { %>
            [ <span style='color:gray'>◀◀</span>]
            [ <span style='color:gray'>◀</span>]
          <% } %>
          &nbsp;&nbsp;
          <% for( var i=startPage; i <= endPage; i++) { %>
            <% if( i != page) { %>
            [ <a href="/list/<%=i%>"> <%=i %> </a> ]
            <% } else { %>
            [ <%=i %>]
            <% } %>
          <% } %>
          &nbsp;&nbsp;
          <% if( endPage < totalPage ) {%>
            [ <a style="text-decoration:none" href="/list/<%=endPage+1%>"> ▶</a>]
            [ <a style="text-decoration:none" href="/list/<%=totalPage%>"> ▶▶</a>]
          <% } else { %>
            [ <span style='color:gray'>▶▶</span>]
            [ <span style='color:gray'>▶</span>]
          <% } %>
      </div>
    </table>
  </body>
</html>
